{
  "name": "dolphin-api-server",
  "version": "0.1.0",
  "description": "The back end api implementation for the site platform",
  "main": "api-server.js",
  "scripts": {
    "docker-build": "rm -rf node_modules && npm i --production && docker-build",
    "docker-run": "docker-run -p 9000:9000 +",
    "start": "echo 'Starting server' & node api-server.js",
    "test": "mocha test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Higino/DolphinProject.git"
  },
  "author": "Higino Silva",
  "license": "ISC",
  "dependencies": {
    "docker-build-run-push": "^3.0.0",
    "express": "^4.15.3"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^5.2.0",
    "request": "^2.87.0",
    "sinon": "^6.0.0"
  }
}
